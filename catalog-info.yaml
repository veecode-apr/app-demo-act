apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: app-demo-act-location
  description: Location to import all app-demo-act catalog components
spec:
  targets:
    - ./.platform/*.yaml
